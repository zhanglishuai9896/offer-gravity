{
  "name": "offer-yinli-static-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node build/write-env.mjs && node build/static-build.mjs",
    "test": "node --test tests/*.test.mjs && npm run build"
  }
}
