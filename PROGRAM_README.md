# Offer 引力程序说明

这是面向全部行业求职者的 AI 求职分析、岗位匹配与投递管理程序。行业和岗位均可自由填写，不限制在预设职业中。

## 已实现

- 保存当前行业／岗位、目标行业／岗位、教育、技能、成果、简历和补充材料；
- AI 分析个人情况、求职准备度、优势、缺口和适合方向；
- 生成本轮求职策略和下一步行动；
- 粘贴职位描述并生成投递建议；
- 核对年限、学历、地点等硬条件；
- 给出匹配证据、风险、材料调整建议和招呼语；
- 保存岗位并维护投递阶段；
- 支持招聘平台、企业官方申请页和招聘邮箱三类投递渠道；
- 自动准备招呼语和岗位材料，打开目标页面或邮件草稿，并进入“待确认”；
- 统计投递、有效回复和面试邀约；
- 导出、导入和删除本地数据；
- 无 API Key 时使用本地规则，有 Key 时使用服务端 AI 分析；
- AI 调用失败时自动回退到本地模式。

程序不会保存招聘平台密码，也不会绕过验证码或平台风控。对于具有官方授权接口的渠道，可以在后续接入自动发送；对于限制第三方自动操作的平台，程序自动准备材料并打开申请页，由用户确认最后一步。

## 启动

需要 Node.js 18 或更高版本。

在当前 Mac 上，可以直接双击：

```text
启动程序.command
```

程序会自动打开浏览器。运行期间不要关闭随之出现的终端窗口；需要停止时，在终端窗口按 `Control + C`。

也可以在终端启动：

```bash
npm start
```

然后打开：

```text
http://127.0.0.1:4173
```

如果电脑没有全局 Node.js，在当前 Codex 环境可使用：

```bash
/Users/apple/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node server.mjs
```

## 启用 AI 分析

复制 `.env.example` 为 `.env`，在服务端填写自己的 Key：

```text
OPENAI_API_KEY=你的Key
OPENAI_MODEL=gpt-5.6-terra
PORT=4173
```

不要把真实 Key 写进 `public/app.js`、截图或提交到代码仓库。默认选择 `gpt-5.6-terra`，用于平衡分析质量和调用成本；可以通过环境变量替换模型。

## 测试

```bash
npm test
```

## 数据说明

- 用户档案、岗位和投递状态保存在浏览器 `localStorage`；
- 开启 AI 后，完成分析所需的简历与职位文本会发送到配置的模型服务；
- 服务端请求设置为 `store: false`，但正式商业上线前仍需完成隐私政策、用户协议、模型公示／登记和专业法律审核；
- 当前版本适合小规模真实测试，不等同于已经完成生产级安全审计的正式 SaaS。
- “待确认”表示材料已经准备并进入目标渠道，不表示简历已经成功发送；只有得到用户或授权接口的成功确认后才应标记为“已投递”。
