# Offer 引力完整源代码 v4

本文件夹包含：

- 本地 Node.js 完整程序：`server.mjs`、`public/`、`lib/`、`test/`；
- Sites 线上部署版本：`site/`；
- 商业化配置、产品说明、访谈与交付模板。

## 安全说明

- 已排除 `.env`、OpenAI API Key、账号数据、用户简历、运行日志和缓存；
- `.env.example` 只包含空白配置模板；
- 请勿把真实密钥写入前端文件或提交到代码仓库。

## 本地运行

1. 安装 Node.js 22 或更高版本；
2. 在本文件夹运行 `pnpm install`；
3. 将 `.env.example` 复制为 `.env`，只在服务端填写所需密钥；
4. 运行 `pnpm start`；
5. 浏览器打开 `http://127.0.0.1:4173/`。

运行测试：`pnpm test`。
